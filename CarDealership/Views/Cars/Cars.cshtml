@using Cars.Models;

<h1>To Do List</h1>
@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
<ul>
  @foreach (Car item in Cars)
  {
    <li>@item.GetMakeModel, @item.GetMiles, @item.GetPrice</li>
  }
</ul>

