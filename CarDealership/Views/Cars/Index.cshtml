@using CarDealership.Models;

<h1>To Do List</h1>
@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
<ul>
  @foreach (Car item in Model)
  {
    <li>@item.MakeModel, @item.Miles, $ @item.Price</li>
  }
</ul>